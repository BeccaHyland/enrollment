<h1>Edit a Student</h1>

<%= form_for(@student) do |s| %>
  <ul>
  <% @student.errors.full_messages.each do |error| %>
    <li><%= error %></li>
  <% end %>
  </ul>
  <p>
    <%= s.label :name %><br />
    <%= s.text_field :name %>
  </p>
  <p>
    <%= s.submit %>
  </p>
<% end %>
